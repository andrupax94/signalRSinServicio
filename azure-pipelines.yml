trigger:
- main

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:


# Sube los archivos al servidor FTP
- task: FtpUpload@2
  inputs:
    credentialsOption: 'inputs'
    serverUrl: 'ftps://waws-prod-yt1-003.ftp.azurewebsites.windows.net/site/wwwroot'
    username: 'serviciointermedio\$serviciointermedio'
    password: 'cPGZyyzqFkzrzlbzb4ko7EXyhWFzwukFxeGFDtQyKbw9r7su5MKL8cgtZWFi'
    implicitFTPS: true
    rootDirectory: '$(Pipeline.Workspace)' # Carpeta raíz donde se ubican los archivos del artefacto generado
    filePatterns: '**' # Patrón para incluir todos los archivos
    remoteDirectory: '/' # Ruta remota en el servidor FTP donde se copiarán los archivos
  
